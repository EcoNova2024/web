import RegisterPage from "@/pages/auth/register"
import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Kayıt Ol",
}

const Page = () => {
  return <RegisterPage />
}

export default Page
